# 🐴 北马AI工具包 - 开发步骤操作文档

**北马AI·与你同在 😊**

## 📌 快速导航

- [环境准备](#环境准备)
- [项目初始化](#项目初始化)
- [前端开发](#前端开发)
- [后端开发](#后端开发)
- [完整开发流程](#完整开发流程)
- [常见问题](#常见问题)

---

## 🛠️ 环境准备

### 系统要求

| 工具 | 版本 | 说明 |
|------|------|------|
| Node.js | >= 20.x | JavaScript 运行时 |
| pnpm | >= 10.x | 包管理器(必须使用 pnpm) |
| Rust | >= 1.85.0 | 系统编程语言 |
| Tauri CLI | 2.x | 跨平台应用框架 |

### 1️⃣ 安装 Node.js

**Windows:**
```bash
# 使用 nvm-windows 管理 Node.js 版本
# 或直接下载 https://nodejs.org/
node --version  # 验证版本 >= 20.x
npm --version
```

**macOS/Linux:**
```bash
# 使用 nvm 管理
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 20
nvm use 20
node --version
```

### 2️⃣ 安装 pnpm

```bash
# 全局安装 pnpm
npm install -g pnpm@10.10.0

# 验证安装
pnpm --version  # 应该输出 10.10.0 或以上
```

### 3️⃣ 安装 Rust

**Windows/macOS/Linux:**
```bash
# 下载并运行 rustup 安装脚本
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# 按提示选择 "1) Proceed with installation"

# 验证安装
rustc --version
cargo --version
```

### 4️⃣ 安装 Tauri CLI

```bash
# 全局安装 Tauri CLI
pnpm install -g @tauri-apps/cli@latest

# 验证安装
tauri --version
```

---

## 📂 项目初始化

### 克隆项目

```bash
# 使用 HTTPS 克隆
git clone https://github.com/bayma888/bmai-tools.git
cd bmai-tools

# 或使用 SSH(需要配置 SSH key)
git clone git@github.com:bayma888/bmai-tools.git
cd bmai-tools
```

### 安装前端依赖

```bash
# 安装所有依赖(包括前端和后端)
pnpm install

# 等待安装完成,可能需要 5-10 分钟
```

### 验证环境

```bash
# 检查前端依赖
pnpm list react

# 检查 TypeScript
pnpm typecheck

# 检查 Rust 环境(在 src-tauri 目录)
cd src-tauri
cargo --version
cargo build --help
cd ..
```

---

## 💻 前端开发

### 只开发前端的场景

当你只需要修改 UI 界面、样式、交互逻辑时,可以单独启动前端开发服务器。

**优点:**
- 🚀 快速启动(无需编译 Rust)
- 🔄 热更新立即生效
- ⚡ 开发效率高

### 📋 前端开发步骤

#### 步骤 1: 启动前端开发服务器

```bash
# 在项目根目录执行
cd D:\xiaobei_Work\GitHub\bmai-tools

# 方式一: 仅启动前端 Vite 服务器(推荐,最快)
pnpm dev:renderer

# 方式二: 启动完整开发环境(需要等待 Rust 编译)
pnpm dev
```

**预期输出:**
```
  VITE v5.0.0  ready in 123 ms

  ➜  Local:   http://localhost:5173/
  ➜  press h to show help
```

#### 步骤 2: 浏览器访问

```
http://localhost:5173/
```

#### 步骤 3: 修改代码并查看变化

编辑前端文件,浏览器会自动热更新:

```bash
# 修改样式
vim src/index.css

# 修改组件
vim src/components/providers/ProviderList.tsx

# 修改文案(国际化)
vim src/i18n/locales/zh.json
```

#### 步骤 4: TypeScript 类型检查

```bash
# 检查类型错误
pnpm typecheck

# 自动格式化代码
pnpm format

# 检查代码格式
pnpm format:check
```

#### 步骤 5: 运行单元测试

```bash
# 运行所有测试
pnpm test:unit

# 监听模式运行测试(修改代码自动重新测试)
pnpm test:unit:watch
```

### ⚠️ 前端开发注意事项

1. **API 调用问题** - 使用 `pnpm dev:renderer` 时,无法调用后端 Rust API,仅在浏览器 console 中测试 UI
2. **需要后端数据** - 使用 `pnpm dev` 启动完整开发环境
3. **样式更新** - TailwindCSS 会自动编译,保存即生效
4. **国际化** - 修改 `src/i18n/locales/` 下的 JSON 文件后自动刷新

### 🎨 常见前端修改位置

| 位置 | 说明 |
|------|------|
| `src/components/` | UI 组件文件 |
| `src/index.css` | 全局样式 |
| `tailwind.config.js` | Tailwind CSS 配置 |
| `src/i18n/locales/` | 国际化文件 |
| `src/hooks/` | React 自定义 Hooks |
| `src/lib/` | 工具函数库 |

---

## ⚙️ 后端开发

### 只开发后端的场景

当你需要修改业务逻辑、数据库操作、API 接口时,需要单独开发后端。

**适用情况:**
- 修改数据库 schema
- 新增 Tauri 命令
- 修改文件读写逻辑
- 优化性能
- 修复后端 Bug

### 📋 后端开发步骤

#### 步骤 1: 进入 Rust 项目目录

```bash
cd src-tauri

# 验证 Rust 环境
cargo --version
rustc --version
```

#### 步骤 2: 修改后端代码

```bash
# 查看项目结构
ls src/

# 编辑后端文件
vim src/main.rs
vim src/commands/provider.rs
vim src/database/mod.rs
```

#### 步骤 3: 编译后端

```bash
# 开发模式编译(包含调试符号)
cargo build

# 生产模式编译(优化大小和性能)
cargo build --release
```

**预期输出:**
```
Compiling bmai_tools_lib v3.8.0
Finished dev [unoptimized + debuginfo] target(s) in 2.34s
```

#### 步骤 4: 检查代码质量

```bash
# 代码检查(找出潜在问题)
cargo clippy

# 格式化 Rust 代码
cargo fmt

# 验证格式
cargo fmt -- --check
```

#### 步骤 5: 运行后端测试

```bash
# 运行所有 Rust 测试
cargo test

# 运行特定测试
cargo test test_name

# 输出详细信息
cargo test -- --nocapture
```

#### 步骤 6: 查看文档

```bash
# 生成并打开文档
cargo doc --open
```

### 🔍 后端常见修改位置

| 目录 | 说明 |
|------|------|
| `src/main.rs` | 应用入口点 |
| `src/commands/` | Tauri 命令(API 接口) |
| `src/database/` | 数据库操作 |
| `src/services/` | 业务逻辑服务 |
| `Cargo.toml` | Rust 依赖配置 |

### 🧪 后端开发注意事项

1. **编译时间** - 首次编译可能需要 5-10 分钟
2. **缓存问题** - 修改 Cargo.toml 后需要重新编译
3. **增量编译** - 只修改代码后,下次编译会更快(秒级)
4. **错误处理** - 使用 `Result` 处理错误,不要 unwrap

---

## 🚀 完整开发流程

### 场景 1: 前后端同步开发(最常见)

**目标:** 既修改前端页面,又修改后端逻辑

#### 终端 1 - 启动完整开发环境

```bash
cd D:\xiaobei_Work\GitHub\bmai-tools
pnpm dev

# 等待输出类似:
# INFO: Tauri listening on http://127.0.0.1:56519
# 浏览器自动打开 http://localhost:5173/
```

#### 终端 2 - 监听后端变化(可选)

```bash
cd D:\xiaobei_Work\GitHub\bmai-tools\src-tauri
cargo watch -x build

# cargo watch 需要安装: cargo install cargo-watch
```

#### 开发步骤

1. **修改前端代码** → 浏览器自动刷新
2. **修改后端代码** → Rust 重新编译 → 应用自动重新加载
3. **查看效果** → 检查 DevTools 和控制台日志

#### 常见操作

```bash
# 打开浏览器开发工具
# Windows/Linux: F12 或 Ctrl+Shift+I
# macOS: Cmd+Option+I

# 查看应用日志(Rust 后端输出)
# 在 DevTools 的 Console 标签查看
```

### 场景 2: 只调试前端(快速迭代)

#### 单终端启动

```bash
cd D:\xiaobei_Work\GitHub\bmai-tools
pnpm dev:renderer

# 打开 http://localhost:5173/
```

**特点:**
- ✅ 快速启动(无 Rust 编译)
- ✅ 热更新立即生效
- ❌ 无法测试后端 API 调用

#### 测试 UI 的方法

```typescript
// 在浏览器 DevTools Console 中测试

// 模拟 API 响应
const mockProviders = [
  { id: '1', name: 'Claude', apiKey: 'xxx' },
  { id: '2', name: 'OpenAI', apiKey: 'yyy' }
];

// 验证前端逻辑
console.log(mockProviders);
```

### 场景 3: 只调试后端

#### 终端 - 运行后端开发

```bash
cd D:\xiaobei_Work\GitHub\bmai-tools\src-tauri

# 编译并运行
cargo run

# 或使用 watch 自动重编译
cargo watch -x run
```

#### 使用 Rust 调试器

```bash
# 安装调试工具
cargo install cargo-expand
cargo install cargo-tree

# 查看依赖树
cargo tree

# 展开宏
cargo expand
```

#### 日志调试

```rust
// 在 Rust 代码中添加日志
log::info!("信息日志");
log::debug!("调试日志");
log::warn!("警告日志");
log::error!("错误日志");

// 设置日志级别环境变量
// RUST_LOG=debug cargo run
```

---

## 🏗️ 构建与打包

### 开发版本构建

```bash
cd D:\xiaobei_Work\GitHub\bmai-tools

# 构建开发版本(包含调试符号)
pnpm build
```

### 生产版本构建

```bash
# 优化大小和性能
cd src-tauri
cargo build --release
```

### 生成应用安装包

```bash
cd D:\xiaobei_Work\GitHub\bmai-tools

# 生成平台特定的安装包
pnpm tauri build

# 输出文件位置
# Windows: src-tauri/target/release/bundle/msi/
# macOS: src-tauri/target/release/bundle/macos/
# Linux: src-tauri/target/release/bundle/appimage/
```

---

## 🐛 常见问题

### Q1: pnpm dev 启动失败

**错误信息:** `command not found: pnpm`

**解决方案:**
```bash
# 检查 pnpm 是否安装
pnpm --version

# 如果未安装
npm install -g pnpm@10.10.0

# 重新尝试
pnpm install
pnpm dev
```

### Q2: Rust 编译错误

**错误信息:** `error: could not compile ...`

**解决方案:**
```bash
# 清理编译缓存
cd src-tauri
cargo clean
cargo build
```

### Q3: 前端热更新不工作

**症状:** 修改代码后浏览器不自动刷新

**解决方案:**
```bash
# 检查 Vite 是否正常运行
# 查看终端输出是否有错误信息

# 手动刷新浏览器
# F5 或 Ctrl+R

# 清理浏览器缓存
# DevTools -> Network -> 勾选 "Disable cache"
```

### Q4: 后端 API 调用无法工作

**症状:** 前端调用后端命令返回 404

**解决方案:**
```bash
# 确保使用完整开发环境
pnpm dev  # 不是 pnpm dev:renderer

# 检查后端是否成功编译
# 查看终端输出,确保没有 Rust 编译错误

# 查看浏览器 DevTools Console 了解详细错误
```

### Q5: 端口被占用

**错误信息:** `Port 5173 is in use`

**解决方案:**
```bash
# Windows - 查找占用端口的进程
netstat -ano | findstr :5173

# 杀死进程(需要管理员权限)
taskkill /PID <PID> /F

# macOS/Linux
lsof -i :5173
kill -9 <PID>
```

### Q6: 类型错误太多

**症状:** TypeScript 报告数百个类型错误

**解决方案:**
```bash
# 重新安装依赖
rm -rf node_modules pnpm-lock.yaml
pnpm install

# 重新生成类型定义
pnpm typecheck --noEmit
```

---

## 📚 快速命令参考

### 前端相关
```bash
pnpm dev:renderer        # 仅启动前端开发服务器
pnpm typecheck           # 类型检查
pnpm format              # 自动格式化代码
pnpm test:unit           # 运行单元测试
pnpm test:unit:watch     # 监听模式运行测试
pnpm build:renderer      # 构建前端
```

### 后端相关
```bash
# 进入后端目录
cd src-tauri

cargo build              # 编译(开发模式)
cargo build --release    # 编译(生产模式)
cargo clippy             # 代码检查
cargo fmt                # 代码格式化
cargo test               # 运行测试
cargo doc --open         # 生成并打开文档
```

### 完整应用
```bash
pnpm dev                 # 启动完整开发环境
pnpm build               # 构建应用
pnpm tauri build         # 生成安装包
```

---

## 🎯 开发最佳实践

1. **使用 pnpm dev:renderer 快速调试前端** - 最快的开发体验
2. **定期运行 pnpm typecheck** - 及时发现类型错误
3. **使用 cargo clippy 检查 Rust 代码** - 提高代码质量
4. **写清晰的提交信息** - 便于团队协作
5. **在修改前创建新分支** - 保持 main 分支干净
6. **运行测试后再提交** - 确保代码质量

---

## 🤝 需要帮助?

- 📖 查看 [README.md](../README.md)
- 📝 查看 [CHANGELOG.md](../CHANGELOG.md)
- 🐛 提交 [Issue](https://github.com/bayma888/bmai-tools/issues)
- 💬 加入 [讨论](https://github.com/bayma888/bmai-tools/discussions)

---

<div align="center">

**🐴 北马AI·与你同在 😊**

Happy coding! 🚀

</div>
